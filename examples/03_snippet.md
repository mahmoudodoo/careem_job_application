# Code Review Assistant

| | |
|---|---|
| Reviewed | `samples/snippet.go` |
| Provider | `mock` |
| Model | `offline-heuristics` |
| Elapsed | 0.0s |

> Offline mock mode: findings come from the deterministic static pass only. No model was called.

**Language:** go

## Three improvements

### 1. Loop reads one element past the end

Loop bound uses `<= len(...)`, so the final iteration indexes one past the end and panics at runtime.

**Before**

```
for i := 0; i <= len(etas); i++ {
```

**After**

```
for i := 0; i < len(etas); i++ {
```

### 2. Division by zero on empty input

`AverageETA` divides by a collection length with no empty-input guard; an empty input divides by zero.

**Before**

```
return total / len(etas)
```

**After**

```
// Return early for the empty case with a typed error or a documented zero value.
return total / len(etas)
```

### 3. No test covers this behaviour

Nothing here pins the current behaviour down, so the next change to it is unverifiable and any regression reaches production silently.

**Before**

```
(no test file alongside this code)
```

**After**

```
Add one table-driven test covering the empty input, the single-element input, and the failure path.
```

## One positive note

`AverageETA` is only 7 lines and takes its inputs as plain parameters, so it can be tested without standing anything up. That is the property that makes every other issue here cheap to fix.

---

**Verdict:** Do not merge yet - at least one issue here will fail at runtime. (offline mock mode - deterministic static analysis only, no model call)

<sub>Generated by careem-ai-reviewer.</sub>
